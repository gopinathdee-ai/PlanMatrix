/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
export async function seed(knex) {
  // TODO: Add optional test/demo seeding data here
  // This data is only used for testing and development
  // Examples: sample users, sample data, test fixtures

  // Example:
  // await knex('users').insert([
  //   { id: 2, name: 'Test User', email: 'test@example.com' },
  //   { id: 3, name: 'Demo User', email: 'demo@example.com' }
  // ]);
}
